# Backtest Implementation Notes

This note documents how the app builds backtest data files.

## World Cup backtest

Script:

```bash
npm run backtest:worldcup
```

Source:

```text
/Users/cashmcdearis/Downloads/WorldCupMatches.csv
```

Output:

```text
src/worldCupBacktestData.js
```

Method:

1. Parse historical matches.
2. Sort chronologically.
3. Build rolling team goal/form ratings from prior matches only.
4. Predict home/draw/away with a Poisson/Dixon-Coles style model.
5. Apply the v1.2 draw guardrail: soft cap high draw probabilities and discount draws that barely lead the best side.
6. Compare predicted outcome against actual result.
7. Save summary, confidence buckets, tournament summary, miss analysis, and recent rows.

Limit:

- no historical odds,
- no ROI,
- no closing-line value.

## MLB Statcast backtest

Script:

```bash
npm run backtest:mlb
```

Source:

```text
/Users/cashmcdearis/Downloads/Data_MLB_2025_StatcastPostseason_PitchByPitch_20251102a.csv
```

Output:

```text
src/mlbBacktestData.js
```

Method:

1. Parse pitch-by-pitch rows.
2. Group rows by `game_pk`.
3. Derive final game score.
4. Build rolling team features from prior games only.
5. Predict home/away winner.
6. Apply v1.3-style MLB compression and rolling after-loss prior.
7. Compare prediction against actual winner.
8. Save summary, miss analysis, matchup splits, confidence splits, and recent rows.

Derived features:

- run differential,
- xwOBA for,
- xwOBA allowed,
- hard-hit rate,
- hard-hit allowed,
- whiff rate,
- whiff generated by pitching,
- CSW rate,
- cold-start flag.

Limit:

- postseason-only,
- no historical odds,
- no starting-pitcher explicit field,
- no confirmed lineups,
- no weather/park adjustment,
- no ROI.

## App integration

The Backtest tab imports:

```js
import { worldCupBacktest } from "./worldCupBacktestData.js";
import { mlbBacktest } from "./mlbBacktestData.js";
```

The generated files are local static data files.

They are regenerated manually with npm scripts.

## Algorithm v1.2 calibration

The generated backtest findings now feed `computeEventModel()` through a calibration layer:

- Soccer: draw priors are anchored toward the market, soft-capped, and discounted when Draw is only barely ahead.
- Soccer: low-confidence reads reduce model blend weight.
- MLB: raw model probabilities are compressed toward 50/50.
- MLB: high-confidence and away-favorite reads reduce model blend weight.

This happens before the market/no-vig blend, so Value, Algorithm, Bankroll Watch, and Model Lab tracking all see the same corrected probability.

## Algorithm v1.3 after-loss prior

The MLB after-loss research now feeds the live model:

- The Odds API `/scores` is fetched on live MLB refresh.
- Completed MLB scores become `mlbLastResults`, keyed by normalized team name.
- `mlbLastResults` is persisted with the other local stats.
- `mlbModel()` applies a small capped logit nudge only when a team lost its previous completed game and has a TeamRankings after-loss win rate.

Formula:

```text
afterLossLogit = clamp((teamAfterLossWinPct - 0.500) * 0.60, -0.08, +0.08)
```

This is deliberately conservative because the historical MLB odds dataset showed the market is already highly calibrated.

## Future baseline import

Script:

```bash
npm run import:future-baseline
```

Source:

```text
/Users/cashmcdearis/Downloads/future_match_probabilities_baseline.csv
```

Output:

```text
src/futureMatchBaselineData.js
```

Method:

1. Parse 2026 group match probability rows.
2. Preserve Elo, injury flags, and home/draw/away probabilities.
3. Generate group summaries.
4. Feed rows into `matchResearchModels` as external priors.

Limit:

- no sportsbook odds,
- no ROI,
- playoff placeholder teams are lower confidence.
